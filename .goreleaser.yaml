version: 2
before:
  hooks:
  - go mod tidy
  - go generate ./...
builds:
- env:
  - CGO_ENABLED=0
  goos:
  - linux
  - windows
  - darwin
  main: main.go
  goarch:
  - amd64
  binary: limetrend
  id: limetrend
  ldflags:
  - -s -w
    -X "github.com/gkwa/limetrend/version.Version={{ .Version }}"
    -X "github.com/gkwa/limetrend/version.Date={{ .CommitDate }}"
    -X "github.com/gkwa/limetrend/version.GoVersion={{ .Env.GOVERSION }}"
    -X "github.com/gkwa/limetrend/version.ShortGitSHA={{ .ShortCommit }}"
    -X "github.com/gkwa/limetrend/version.FullGitSHA={{ .FullCommit }}"
archives:
- format: tar.gz
  name_template: |-
    limetrend_ {{- title .Os }}_ {{- if eq .Arch "amd64" }}x86_64
                               {{- else if eq .Arch "386" }}i386
                               {{- else }}
                               {{ .Arch }}
                               {{ end }}
                               {{- if .Arm }}v{{ .Arm }}{{ end }}
  format_overrides:
  - goos: windows
    format: zip
checksum:
  name_template: checksums.txt
snapshot:
  name_template: '{{ incpatch .Version }}-next'
changelog:
  sort: asc
  filters:
    exclude:
    - '^docs:'
    - '^test:'
brews:
- name: limetrend
  directory: Formula
  homepage: https://github.com/gkwa/limetrend
  repository:
    owner: gkwa
    name: homebrew-tools
    branch: main
scoops:
- name: limetrend
  homepage: https://github.com/gkwa/limetrend
  description: "Description of the limetrend project"
  license: MIT
  repository:
    owner: gkwa
    name: scoop-bucket
    branch: master
